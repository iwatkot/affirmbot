{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Linux / Mac  Bot",
            "type": "debugpy",
            "request": "launch",
            "program": "src/bot.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            // "preLaunchTask": "mypy",
            "env": {
                "PYTHONPATH": "${workspaceFolder}:${PYTHONPATH}"
            }
        }
    ]
}